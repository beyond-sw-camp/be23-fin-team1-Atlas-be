apiVersion: v1
kind: Service
metadata:
  name: control-service
  namespace: atlas
spec:
  selector:
    app: control-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
