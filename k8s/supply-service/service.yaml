apiVersion: v1
kind: Service
metadata:
  name: supply-service
  namespace: atlas
spec:
  selector:
    app: supply-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
