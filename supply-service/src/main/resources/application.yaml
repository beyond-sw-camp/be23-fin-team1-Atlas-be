spring:
  profiles:
    active: local
  application:
    name: supply-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,mappings
  endpoint:
    health:
      probes:
        enabled: true