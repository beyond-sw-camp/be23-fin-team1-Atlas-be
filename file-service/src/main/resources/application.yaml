spring:
  profiles:
    active: local
  application:
    name: file-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true